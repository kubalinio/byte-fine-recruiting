# Byte Fine Recruiting

## Run tasks

First, install the dependencies:

```sh
pnpm install
```

To run the dev server for your app, use:

```sh
pnpm dev
```

To create a production bundle:

```sh
pnpm build
```
